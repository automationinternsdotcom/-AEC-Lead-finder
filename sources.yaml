# Deprecated.
#
# Article discovery now loads website scrape sources from news_websites.csv via
# pipeline.config.load_sources(). This file is intentionally ignored by the daily
# fetch path so the pipeline does not fall back to Google News search RSS.
