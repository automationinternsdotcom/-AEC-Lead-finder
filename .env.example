# Pipedrive (required)
PIPEDRIVE_API_TOKEN=
PIPEDRIVE_DOMAIN=
PIPEDRIVE_PIPELINE_ID=
PIPEDRIVE_STAGE_ID=
PIPEDRIVE_FIELD_ARTICLE_URL=

# Apollo (optional — if unset, deals create with lead_gap=True).
APOLLO_API_KEY=

# Optional — set to 1 for dry-run mode (no Pipedrive writes).
DRY_RUN=0
