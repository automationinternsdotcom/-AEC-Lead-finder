[project]
name = "aether-leads"
version = "0.1.0"
description = "Simple lead intelligence pipeline for Aether Facility Services"
requires-python = ">=3.12"
dependencies = [
    "anthropic>=0.40",
    "feedparser>=6.0",
    "httpx>=0.27",
    "pydantic>=2.8",
    "python-dotenv>=1.0",
    "trafilatura>=1.12",
]

[tool.uv]
package = false
